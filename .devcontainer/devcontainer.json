{
  "name": "Python 3",
  // Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "crispr_app/main.py"
      ]
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "updateContentCommand": "[ -f packages.txt ] && sudo apt update && sudo apt upgrade -y && sudo xargs apt install -y <packages.txt; pip3 install --user -r crispr_app/requirements.txt pytest; echo '✅ Packages installed and Requirements met'",
  "postAttachCommand": {
    "server": "uvicorn main:app --host 0.0.0.0 --port 8000 --app-dir crispr_app"
  },
  "portsAttributes": {
    "8000": {
      "label": "CRISPR Precision Studio",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8000
  ]
}